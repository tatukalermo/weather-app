/* eslint-disable no-undef */
/* eslint-disable semi */
/* eslint-disable react/jsx-filename-extension */
describe("Something", () => {
  it("renders the Weather wrapper ", () => {
    const numb = 1;
    expect(numb).equal(1);
  });
});
